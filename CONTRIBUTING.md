# Contributing

1. Keep target-specific configuration out of source control.
2. Run `python -m py_compile android_automation_engine.py device_controller.py` before submitting changes.
3. Preserve the separation between the controller and automation engine.
4. Add page-state profiles only from authorized test environments.
5. Do not commit APKs, service-account files, screenshots containing private data or runtime logs.
